listener 1883
protocol mqtt

listener 9001
protocol websockets

# 未決定事項: 認証方式(ユーザー名/パスワード, TLS)が決まるまでは
# 同一Wi-Fiネットワーク内限定運用を前提に匿名接続を許可する
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log
log_dest stdout
